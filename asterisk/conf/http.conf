[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088

; WebRTC signalling (transport-wss in pjsip.conf) rides over this same HTTP
; server. For anything beyond localhost testing, put a TLS-terminating
; reverse proxy (nginx/caddy) in front and point browsers at wss:// through
; it — browsers refuse getUserMedia on insecure origins other than
; localhost. See README "Known tradeoffs".
