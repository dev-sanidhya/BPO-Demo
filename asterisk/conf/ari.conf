[general]
enabled = yes
pretty = yes
allowed_origins = *

; Used by services/realtime-assist (ari_listener.py) and
; scripts/make_test_call.py to control calls and tap live audio.
; Change this password before ever exposing 8088 beyond localhost.
[asterisk]
type = user
read_only = no
password = changeme_ari_password
